---
phase: 02-storage-access-and-diagnostics
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/Modules/Catalog/DTOs/AdminStorageDiagnosticsDtos.cs
  - apps/api/Modules/Catalog/Controllers/AdminStorageDiagnosticsController.cs
  - apps/api/Modules/Catalog/Services/IStorageDiagnosticsService.cs
  - apps/api/Modules/Catalog/Services/StorageDiagnosticsService.cs
  - apps/api/Modules/Catalog/Repositories/IStorageDiagnosticsRepository.cs
  - apps/api/Modules/Catalog/Repositories/DapperStorageDiagnosticsRepository.cs
  - apps/api/Modules/Catalog/Repositories/StorageDiagnosticsSql.cs
  - apps/api/Modules/Catalog/CatalogServiceCollectionExtensions.cs
autonomous: true
requirements:
  - STOR-03
user_setup: []
must_haves:
  truths:
    - "D-05: Diagnostics are exposed as a backend admin endpoint protected for staff/admin access; frontend UI is not required in Phase 2."
    - "D-06: The report includes summary/counts plus bounded detail lists for missingFiles, untrackedFiles, staleDeletedRows, and orphanedRows."
    - "D-07: Diagnostics compare database rows in stored_files against physical files under Storage:RootPath."
    - "D-08: Diagnostics must not expand to product/brand reference consistency beyond existing constraints and tests."
    - "D-09: Counts represent the full scan result while detail lists are bounded and report truncated=true when applicable."
    - "D-10: Phase 2 diagnostics are read-only and must not delete files, update stored_files, mark rows, or add cleanup endpoints."
    - "D-11: staleDeletedRows means stored_files.status = 'deleted' and the physical file still exists."
    - "D-12: orphanedRows means stored_files.status = 'orphaned' regardless of physical file presence; include fileExists in item details."
    - "D-13: API responses expose storageKey or relative paths only, never absolute server filesystem paths."
  artifacts:
    - path: apps/api/Modules/Catalog/Controllers/AdminStorageDiagnosticsController.cs
      provides: "Read-only staff/admin HTTP endpoint for storage diagnostics"
      contains: "api/admin/storage/diagnostics"
    - path: apps/api/Modules/Catalog/Services/StorageDiagnosticsService.cs
      provides: "DB/disk comparison, bounded details, and summary counts"
      contains: "missingFiles|untrackedFiles|staleDeletedRows|orphanedRows"
    - path: apps/api/Modules/Catalog/Repositories/StorageDiagnosticsSql.cs
      provides: "Read-only stored_files queries for diagnostics"
      contains: "SELECT"
  key_links:
    - from: apps/api/Modules/Catalog/CatalogServiceCollectionExtensions.cs
      to: apps/api/Modules/Catalog/Services/StorageDiagnosticsService.cs
      via: "Scoped service and repository registration"
      pattern: "IStorageDiagnosticsService|IStorageDiagnosticsRepository"
    - from: apps/api/Infrastructure/Database/IDbConnectionFactory.cs
      to: apps/api/Modules/Catalog/Repositories/DapperStorageDiagnosticsRepository.cs
      via: "Dapper read-only stored_files query"
      pattern: "OpenConnectionAsync"
    - from: apps/api/Modules/Catalog/Services/AdminCatalogStaffGuard.cs
      to: apps/api/Modules/Catalog/Services/StorageDiagnosticsService.cs
      via: "Admin/staff authorization gate"
      pattern: "RequireStaffAsync"
---

<objective>
Add a read-only admin storage diagnostics report that compares `stored_files` rows with physical files under Local FileStorage.

User story: As a staff/admin operator, I want a bounded diagnostics endpoint for storage drift so that missing files, untracked files, stale deleted rows, and orphaned rows are visible before release.

Purpose: Cover `STOR-03` and Phase 2 decisions D-05 through D-13 without adding cleanup behavior, import staging, frontend UI, or absolute filesystem path exposure.
Output: DTOs, service/repository implementation, DI registration, and a protected `GET /api/admin/storage/diagnostics` endpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-storage-access-and-diagnostics/02-CONTEXT.md
@.planning/phases/02-storage-access-and-diagnostics/02-RESEARCH.md
@.planning/phases/01-release-safety-baseline/01-VERIFICATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@apps/api/Infrastructure/Database/IDbConnectionFactory.cs
@apps/api/Infrastructure/Storage/LocalStoredFileWriter.cs
@apps/api/Infrastructure/Storage/LocalStoredFileOptions.cs
@apps/api/Modules/Catalog/CatalogServiceCollectionExtensions.cs
@apps/api/Modules/Catalog/Services/AdminCatalogStaffGuard.cs
@apps/api/Modules/Catalog/Controllers/AdminCatalogBrandsController.cs
@apps/dbmigrator/Migrations/002_catalog_foundation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define diagnostics DTO contract with relative paths only</name>
  <files>apps/api/Modules/Catalog/DTOs/AdminStorageDiagnosticsDtos.cs</files>
  <read_first>.planning/phases/02-storage-access-and-diagnostics/02-CONTEXT.md, apps/dbmigrator/Migrations/002_catalog_foundation.sql</read_first>
  <action>Create DTO records for the storage diagnostics response under the catalog admin DTO namespace. Include a summary object with full counts for `missingFiles`, `untrackedFiles`, `staleDeletedRows`, and `orphanedRows`. Include bounded list sections for each category with `items`, `count`, and `truncated`. Missing-file, stale-deleted, and orphaned row items should expose `id`, `storageKey`, `purpose`, `status`, and relevant metadata; orphaned row items must include `fileExists`. Untracked file items should expose only a relative storage key or relative path. Do not include absolute physical paths.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~StorageDiagnostics"</verify>
  <acceptance_criteria>
    - DTO names and JSON property names clearly represent `missingFiles`, `untrackedFiles`, `staleDeletedRows`, and `orphanedRows`.
    - Summary counts are separate from bounded item lists.
    - No DTO property exposes `absolutePath`, `physicalPath`, or equivalent server filesystem detail.
  </acceptance_criteria>
  <done>Storage diagnostics has a stable read-only API response contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add read-only stored_files diagnostics repository</name>
  <files>apps/api/Modules/Catalog/Repositories/IStorageDiagnosticsRepository.cs, apps/api/Modules/Catalog/Repositories/DapperStorageDiagnosticsRepository.cs, apps/api/Modules/Catalog/Repositories/StorageDiagnosticsSql.cs</files>
  <read_first>apps/api/Infrastructure/Database/IDbConnectionFactory.cs, apps/api/Modules/Catalog/Repositories/AdminCatalogImageSql.cs, apps/api/Modules/Catalog/Repositories/AdminCatalogBrandSql.cs, apps/dbmigrator/Migrations/002_catalog_foundation.sql</read_first>
  <action>Add a Dapper repository that reads the `stored_files` table for diagnostics. Keep SQL read-only: `SELECT` statements only. Return enough fields for the service to classify missing files, stale deleted rows, and orphaned rows: id, storage_key, purpose, status, size_bytes, checksum, created_at. Do not add update/delete SQL and do not check product/brand reference consistency in this phase.</action>
  <verify>rg \"UPDATE|DELETE|INSERT\" apps/api/Modules/Catalog/Repositories/StorageDiagnosticsSql.cs; dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter \"FullyQualifiedName~StorageDiagnostics\"</verify>
  <acceptance_criteria>
    - `StorageDiagnosticsSql.cs` contains read-only stored_files queries only.
    - Repository uses `IDbConnectionFactory` and Dapper patterns consistent with catalog repositories.
    - No migration is added for Phase 2 diagnostics.
  </acceptance_criteria>
  <done>Diagnostics can read stored file metadata without mutating database state.</done>
</task>

<task type="auto">
  <name>Task 3: Implement DB/disk comparison service with bounded details</name>
  <files>apps/api/Modules/Catalog/Services/IStorageDiagnosticsService.cs, apps/api/Modules/Catalog/Services/StorageDiagnosticsService.cs</files>
  <read_first>apps/api/Infrastructure/Storage/LocalStoredFileOptions.cs, apps/api/Infrastructure/Storage/LocalStoredFileWriter.cs, apps/api/Modules/Catalog/Services/AdminCatalogStaffGuard.cs, .planning/phases/02-storage-access-and-diagnostics/02-CONTEXT.md</read_first>
  <action>Implement a service that resolves the effective storage root using the same semantics as `LocalStoredFileWriter`/static serving, scans physical files under the storage root, normalizes them to storage keys with the `storage/` prefix, and compares them to repository rows. Classify: `missingFiles` as non-deleted DB rows whose physical file is missing; `untrackedFiles` as physical files without a matching `stored_files.storage_key`; `staleDeletedRows` as rows with `status = 'deleted'` whose physical file still exists; `orphanedRows` as rows with `status = 'orphaned'` regardless of file presence, including `fileExists`. Counts must reflect the full result; item lists must be bounded by a deterministic limit such as default `maxItems = 100`, with `truncated = true` when count exceeds item length. Sort output deterministically by storage key/id. Do not delete files or update database rows.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~StorageDiagnostics"</verify>
  <acceptance_criteria>
    - Counts are computed before item-list truncation.
    - `maxItems` is bounded and deterministic.
    - Item paths use storage keys or relative paths, not absolute paths.
    - Service has no calls to `File.Delete`, repository update methods, or cleanup commands.
  </acceptance_criteria>
  <done>Storage diagnostics report accurately describes DB/disk drift without modifying either side.</done>
</task>

<task type="auto">
  <name>Task 4: Expose protected GET admin diagnostics endpoint and register services</name>
  <files>apps/api/Modules/Catalog/Controllers/AdminStorageDiagnosticsController.cs, apps/api/Modules/Catalog/CatalogServiceCollectionExtensions.cs</files>
  <read_first>apps/api/Modules/Catalog/Controllers/AdminCatalogBrandsController.cs, apps/api/Modules/Catalog/CatalogServiceCollectionExtensions.cs, apps/api/Modules/Catalog/Services/AdminCatalogStaffGuard.cs</read_first>
  <action>Add `[Authorize]` controller route `api/admin/storage/diagnostics` with a `GET` action that delegates to the diagnostics service. Accept an optional bounded `maxItems` query parameter only if implemented with validation/clamping; otherwise use the service default. Ensure staff/admin authorization is enforced through the existing guard path, not frontend-only visibility. Register the diagnostics service and repository in `AddCatalogModule`. Do not add POST/DELETE/PATCH cleanup endpoints and do not require CSRF for the read-only GET endpoint.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~StorageDiagnostics"</verify>
  <acceptance_criteria>
    - Endpoint path is `GET /api/admin/storage/diagnostics`.
    - Anonymous requests are unauthorized and customer role requests are forbidden once endpoint tests are added in Plan 02-03.
    - Seller/admin access goes through backend authorization.
    - There are no destructive storage diagnostics endpoints.
  </acceptance_criteria>
  <done>Read-only storage diagnostics are available to staff/admin through the backend API.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~StorageDiagnostics"` passes after Plan 02-03 tests are present, or this plan summary records that focused tests are delivered by dependent Plan 02-03.
- [ ] `rg "File.Delete|DELETE|UPDATE|INSERT" apps/api/Modules/Catalog/Services/StorageDiagnosticsService.cs apps/api/Modules/Catalog/Repositories/StorageDiagnosticsSql.cs` shows no diagnostics mutation path other than harmless false positives explicitly documented.
- [ ] Manual inspection confirms response DTOs do not expose absolute filesystem paths.
- [ ] Manual inspection confirms diagnostics do not include product/brand reference consistency checks beyond DB/file drift.
</verification>

<success_criteria>
- STOR-03 is covered by a read-only DB/disk drift report.
- D-05: Diagnostics are exposed through a protected backend admin endpoint.
- D-06: Report contains summary/counts plus bounded detail lists.
- D-07: Report compares `stored_files` against physical files under `Storage:RootPath`.
- D-08: Product/brand reference consistency is not expanded in Phase 2.
- D-09: Counts are full, detail lists are bounded and deterministic.
- D-10: Diagnostics are read-only and no cleanup endpoint exists.
- D-11: Stale deleted rows are deleted rows with existing files.
- D-12: Orphaned rows are reported regardless of file presence with `fileExists`.
- D-13: API responses contain only `storageKey` or relative paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-storage-access-and-diagnostics/02-02-SUMMARY.md`.
</output>
