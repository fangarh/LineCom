---
phase: 01-release-safety-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/front/src/lib/seo/site.ts
  - apps/front/src/lib/seo/site.test.ts
  - apps/front/.env.example
  - apps/api/Program.cs
  - apps/api/Infrastructure/Hosting/ProductionConfigurationGuard.cs
  - tests/LineCom.Api.Tests/Infrastructure/Hosting/ProductionConfigurationGuardTests.cs
autonomous: true
requirements:
  - PROD-01
  - PROD-03
user_setup: []
must_haves:
  truths:
    - "D-06: Frontend production build/startup validates LINECOM_PUBLIC_SITE_ORIGIN."
    - "D-07: Production rejects missing, invalid, localhost, 127.0.0.1, and ::1 public site origins."
    - "D-08: Absolute non-localhost staging or preview public site origins remain valid; https://line-com.ru is an example, not a hard requirement."
    - "D-09: LINECOM_API_ORIGIN fallback behavior is not tightened in Phase 1."
    - "D-10: Missing or invalid LINECOM_PUBLIC_SITE_ORIGIN fails fast in production with a clear example message."
    - "API production startup does not silently default Storage:RootPath to the API content root."
  artifacts:
    - path: apps/front/src/lib/seo/site.ts
      provides: "Production-safe public site origin validation for metadataBase, robots, and sitemap helpers"
      contains: "LINECOM_PUBLIC_SITE_ORIGIN must be an absolute non-localhost URL in production"
    - path: apps/front/src/lib/seo/site.test.ts
      provides: "Frontend production origin guard regression tests"
      contains: "localhost"
    - path: apps/api/Infrastructure/Hosting/ProductionConfigurationGuard.cs
      provides: "API production configuration guard for storage root and database connection presence"
      contains: "Storage:RootPath"
  key_links:
    - from: apps/front/src/app/layout.tsx
      to: apps/front/src/lib/seo/site.ts
      via: "metadataBase uses siteMetadataBase during Next production build/startup"
      pattern: "siteMetadataBase"
    - from: apps/front/src/app/robots.ts
      to: apps/front/src/lib/seo/site.ts
      via: "robots host and sitemap use getPublicSiteOrigin/absoluteSiteUrl"
      pattern: "getPublicSiteOrigin|absoluteSiteUrl"
    - from: apps/front/src/app/sitemap.ts
      to: apps/front/src/lib/seo/site.ts
      via: "sitemap origin uses getPublicSiteOrigin"
      pattern: "getPublicSiteOrigin"
    - from: apps/api/Program.cs
      to: apps/api/Infrastructure/Hosting/ProductionConfigurationGuard.cs
      via: "startup validation before service registration depends on config"
      pattern: "ProductionConfigurationGuard"
---

<objective>
Add production configuration guardrails that prevent release SEO URLs and API storage configuration from silently falling back to local defaults.

User story: As a release operator, I want production startup/build checks to fail on unsafe local defaults so that canonical URLs, robots, sitemap output, and storage paths are environment-specific before deployment.

Purpose: Cover decisions D-06 through D-10 and requirements PROD-01/PROD-03 without tightening `LINECOM_API_ORIGIN` in Phase 1.
Output: Frontend public-origin validation, API production storage/database guard, and focused tests documenting allowed and rejected values.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-release-safety-baseline/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@apps/front/src/lib/seo/site.ts
@apps/front/src/lib/seo/site.test.ts
@apps/front/src/app/layout.tsx
@apps/front/src/app/robots.ts
@apps/front/src/app/sitemap.ts
@apps/front/next.config.ts
@apps/front/.env.example
@apps/api/Program.cs
@apps/api/Infrastructure/Database/DatabaseServiceCollectionExtensions.cs
@apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make public site origin fail fast only in production</name>
  <files>apps/front/src/lib/seo/site.ts, apps/front/src/lib/seo/site.test.ts</files>
  <read_first>apps/front/src/lib/seo/site.ts, apps/front/src/lib/seo/site.test.ts, apps/front/src/app/layout.tsx, apps/front/src/app/robots.ts, apps/front/src/app/sitemap.ts, .planning/phases/01-release-safety-baseline/01-CONTEXT.md</read_first>
  <action>Update the SEO site-origin helper so development and test still fall back to `http://127.0.0.1:3000`, but production throws a clear `Error` when `LINECOM_PUBLIC_SITE_ORIGIN` is missing, not an absolute URL, not `http:`/`https:`, or resolves to local host names/addresses `localhost`, `127.0.0.1`, `[::1]`, or `::1`. Preserve normalization that returns `parsed.origin` so paths, query strings, hashes, and trailing slashes are stripped. The production error message must include exactly `LINECOM_PUBLIC_SITE_ORIGIN must be an absolute non-localhost URL in production, e.g. https://line-com.ru`. Do not edit `apps/front/next.config.ts` to hard-require or otherwise tighten `LINECOM_API_ORIGIN`.</action>
  <verify>npm --prefix apps/front test -- src/lib/seo/site.test.ts</verify>
  <acceptance_criteria>
    - Existing development fallback tests still pass.
    - Production tests reject missing value, invalid URL, `localhost`, `127.0.0.1`, and `::1`.
    - Production tests accept `https://line-com.ru` and `https://preview.example.ru`.
    - `apps/front/next.config.ts` still contains the existing `LINECOM_API_ORIGIN ?? "http://127.0.0.1:8080"` fallback.
  </acceptance_criteria>
  <done>Frontend SEO helpers provide production-safe public origins while preserving local development behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Document frontend production origin in env example</name>
  <files>apps/front/.env.example</files>
  <read_first>apps/front/.env.example, .planning/phases/01-release-safety-baseline/01-CONTEXT.md</read_first>
  <action>Add `LINECOM_PUBLIC_SITE_ORIGIN=https://line-com.ru` to `apps/front/.env.example`. Keep the existing `LINECOM_API_ORIGIN=http://127.0.0.1:8080` example because D-09 explicitly says Phase 1 does not tighten `LINECOM_API_ORIGIN`.</action>
  <verify>Get-Content apps/front/.env.example</verify>
  <acceptance_criteria>
    - `.env.example` contains `LINECOM_PUBLIC_SITE_ORIGIN=https://line-com.ru`.
    - `.env.example` still contains `LINECOM_API_ORIGIN=http://127.0.0.1:8080`.
  </acceptance_criteria>
  <done>Frontend environment template makes the required production public origin visible.</done>
</task>

<task type="auto">
  <name>Task 3: Add API production config guard for storage and database defaults</name>
  <files>apps/api/Infrastructure/Hosting/ProductionConfigurationGuard.cs, apps/api/Program.cs, tests/LineCom.Api.Tests/Infrastructure/Hosting/ProductionConfigurationGuardTests.cs</files>
  <read_first>apps/api/Program.cs, apps/api/Infrastructure/Database/DatabaseServiceCollectionExtensions.cs, apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs, apps/api/appsettings.json, .planning/REQUIREMENTS.md</read_first>
  <action>Create a small API production configuration guard under `apps/api/Infrastructure/Hosting`. It must be invoked from `Program.cs` before service registration relies on configuration. In production only, fail clearly when `ConnectionStrings:Default` is missing/blank and when `Storage:RootPath` is missing/blank or not rooted. This prevents production from using the checked-in blank database connection or the local `apps/api/storage` fallback. In development/test, preserve existing fallback behavior so current local tests and dev startup are not broken. Do not add schema migrations and do not change Local FileStorage serving policy; Phase 2 owns storage access boundaries.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~ProductionConfigurationGuard"</verify>
  <acceptance_criteria>
    - Production guard tests cover blank connection string, blank storage root, relative storage root, and valid absolute storage root.
    - Development/test guard path allows existing local fallback behavior.
    - `Program.cs` invokes the guard before `AddDatabase`.
    - No behavior change is made to `/storage` public/private access rules in this plan.
  </acceptance_criteria>
  <done>Production API startup has explicit environment-specific database and storage root checks without changing Phase 2 storage policy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix apps/front test -- src/lib/seo/site.test.ts` passes.
- [ ] `npm --prefix apps/front run build` fails with the expected public-origin error when `NODE_ENV=production` and `LINECOM_PUBLIC_SITE_ORIGIN` is missing, and passes when `LINECOM_PUBLIC_SITE_ORIGIN=https://line-com.ru` with API connectivity mocked or any external API blocker recorded.
- [ ] `dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~ProductionConfigurationGuard"` passes.
- [ ] Inspection confirms `LINECOM_API_ORIGIN` fallback was not tightened in Phase 1.
</verification>

<success_criteria>
- PROD-01 is covered by production public-origin validation for metadataBase, robots, and sitemap helpers.
- PROD-03 is covered for frontend public origin plus API production database/storage default checks.
- D-06: Production frontend validates LINECOM_PUBLIC_SITE_ORIGIN during build/startup paths.
- D-07: Missing, invalid URL, localhost, 127.0.0.1, and ::1 are invalid in production.
- D-08: Non-localhost staging/preview URLs are allowed.
- D-09: LINECOM_API_ORIGIN is not tightened in Phase 1.
- D-10: Bad public origin fails with the exact clear message and example.
- Phase 2 storage access policy remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-safety-baseline/01-02-SUMMARY.md`.
</output>
