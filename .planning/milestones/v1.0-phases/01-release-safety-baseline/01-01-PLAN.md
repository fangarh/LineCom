---
phase: 01-release-safety-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/Program.cs
  - apps/api/Modules/Auth/AuthRateLimiting.cs
  - apps/api/Modules/Auth/Controllers/AuthController.cs
  - tests/LineCom.Api.Tests/Modules/Auth/AuthLoginEndpointTests.cs
  - tests/LineCom.Api.Tests/Modules/Auth/AuthRegisterEndpointTests.cs
  - tests/LineCom.Api.Tests/Modules/Auth/AuthModuleRegistrationTests.cs
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - "D-01: POST /api/auth/login and POST /api/auth/register are the only throttled auth endpoints in Phase 1."
    - "D-02: The throttling key combines remote IP and endpoint path."
    - "D-03: Login/register are throttled after 5 attempts in a 1 minute window."
    - "D-04: Throttled auth requests return HTTP 429 with JSON code auth.rate_limited."
    - "D-05: GET /api/auth/me, POST /api/auth/logout, and request submit endpoints are not included in the Phase 1 throttling policy."
    - "Production cookie authentication remains HttpOnly, SameSite=Lax, and SecurePolicy=Always."
  artifacts:
    - path: apps/api/Modules/Auth/AuthRateLimiting.cs
      provides: "Auth-only fixed-window rate limiter policy and rejection JSON"
      contains: "auth.rate_limited"
    - path: apps/api/Modules/Auth/Controllers/AuthController.cs
      provides: "Endpoint-specific rate limiting metadata on login and register only"
      contains: "EnableRateLimiting"
    - path: tests/LineCom.Api.Tests/Modules/Auth/AuthLoginEndpointTests.cs
      provides: "429 regression coverage for login"
      contains: "auth.rate_limited"
    - path: tests/LineCom.Api.Tests/Modules/Auth/AuthRegisterEndpointTests.cs
      provides: "429 regression coverage for registration"
      contains: "auth.rate_limited"
  key_links:
    - from: apps/api/Program.cs
      to: apps/api/Modules/Auth/AuthRateLimiting.cs
      via: "AddRateLimiter registration and UseRateLimiter middleware"
      pattern: "AddAuthRateLimiting|UseRateLimiter"
    - from: apps/api/Modules/Auth/Controllers/AuthController.cs
      to: "POST /api/auth/login and POST /api/auth/register"
      via: "EnableRateLimiting attribute"
      pattern: "EnableRateLimiting"
---

<objective>
Implement auth abuse protection for the two public credential endpoints.

User story: As a release operator, I want login and registration attempts throttled with a controlled API error so that credential abuse is slowed without changing unrelated auth behavior.

Purpose: Cover Phase 1 decisions D-01 through D-05 and requirement SEC-01 while preserving the existing cookie-auth contract required by SEC-02.
Output: ASP.NET Core rate limiting policy, endpoint annotations, middleware ordering, and endpoint tests for 429 behavior plus production cookie verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-release-safety-baseline/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@apps/api/Program.cs
@apps/api/Modules/Auth/AuthServiceCollectionExtensions.cs
@apps/api/Modules/Auth/Controllers/AuthController.cs
@tests/LineCom.Api.Tests/Modules/Auth/AuthLoginEndpointTests.cs
@tests/LineCom.Api.Tests/Modules/Auth/AuthRegisterEndpointTests.cs
@tests/LineCom.Api.Tests/Modules/Auth/AuthModuleRegistrationTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth-only fixed-window rate limiting policy</name>
  <files>apps/api/Modules/Auth/AuthRateLimiting.cs, apps/api/Program.cs</files>
  <read_first>apps/api/Program.cs, apps/api/Modules/Auth/AuthServiceCollectionExtensions.cs, .planning/phases/01-release-safety-baseline/01-CONTEXT.md</read_first>
  <action>Create `AuthRateLimiting.cs` in namespace `LineCom.Api.Modules.Auth`. Define a public constant policy name for auth attempts. Register ASP.NET Core rate limiting with a fixed window partition keyed by `RemoteIpAddress + endpoint path`, with `PermitLimit = 5`, `Window = TimeSpan.FromMinutes(1)`, and `QueueLimit = 0`. Add an `OnRejected` handler that writes status 429, content type `application/json`, and `ApiErrorResponse("auth.rate_limited", "Слишком много попыток. Попробуйте позже.")`. In `Program.cs`, register the auth rate limiter during service setup and add `app.UseRouting(); app.UseRateLimiter();` before `UseAuthentication()`, following ASP.NET Core endpoint-specific rate limiting middleware order. Keep this policy scoped for explicit endpoint use; do not apply it globally to all controllers.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~Auth"</verify>
  <acceptance_criteria>
    - `AuthRateLimiting.cs` contains `PermitLimit = 5`, `TimeSpan.FromMinutes(1)`, `QueueLimit = 0`, and `auth.rate_limited`.
    - `Program.cs` registers the rate limiter and calls `UseRateLimiter()` before `UseAuthentication()`.
    - The rate limiter partition key includes both remote IP and endpoint path.
  </acceptance_criteria>
  <done>Auth rate limiting service and middleware are registered, but no endpoint is throttled until Task 2 applies the policy.</done>
</task>

<task type="auto">
  <name>Task 2: Apply throttling only to login and register</name>
  <files>apps/api/Modules/Auth/Controllers/AuthController.cs</files>
  <read_first>apps/api/Modules/Auth/Controllers/AuthController.cs, apps/api/Modules/Auth/AuthRateLimiting.cs, .planning/phases/01-release-safety-baseline/01-CONTEXT.md</read_first>
  <action>Add endpoint-specific rate limiting metadata only to `Register` (`POST /api/auth/register`) and `Login` (`POST /api/auth/login`) using the policy constant from `AuthRateLimiting.cs`. Do not add rate limiting metadata to `Me`, `Logout`, or any request-submit endpoint in this phase. Preserve existing controller signatures, response types, CSRF behavior, and auth cookie creation flow.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~Auth"</verify>
  <acceptance_criteria>
    - `Register` and `Login` are the only `AuthController` actions containing `EnableRateLimiting`.
    - `/api/auth/me` and `/api/auth/logout` keep their existing attributes and are not rate-limited by this policy.
  </acceptance_criteria>
  <done>Only the two Phase 1 public credential endpoints use the auth rate limiter.</done>
</task>

<task type="auto">
  <name>Task 3: Add endpoint and cookie production regression tests</name>
  <files>tests/LineCom.Api.Tests/Modules/Auth/AuthLoginEndpointTests.cs, tests/LineCom.Api.Tests/Modules/Auth/AuthRegisterEndpointTests.cs, tests/LineCom.Api.Tests/Modules/Auth/AuthModuleRegistrationTests.cs</files>
  <read_first>tests/LineCom.Api.Tests/Modules/Auth/AuthLoginEndpointTests.cs, tests/LineCom.Api.Tests/Modules/Auth/AuthRegisterEndpointTests.cs, tests/LineCom.Api.Tests/Modules/Auth/AuthModuleRegistrationTests.cs, apps/api/Modules/Auth/AuthServiceCollectionExtensions.cs</read_first>
  <action>Add tests that issue 6 repeated POST requests to `/api/auth/login` and `/api/auth/register` from the same test client and assert that the throttled response is 429 with JSON `{ code: "auth.rate_limited" }`. Keep the login/register fake services deterministic so the first five attempts do not fail because of unrelated dependencies. Add or extend a production cookie-options test proving `AddAuthModule` uses HttpOnly, SameSite=Lax, and `CookieSecurePolicy.Always` when the host environment is production. Keep existing successful login/register and logout tests passing.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~Auth"</verify>
  <acceptance_criteria>
    - Login throttling test fails if the 6th repeated login attempt is not 429.
    - Register throttling test fails if the 6th repeated register attempt is not 429.
    - Tests assert `auth.rate_limited` from response JSON, not only the HTTP status.
    - Production cookie test proves SecurePolicy=Always without weakening development cookie behavior.
  </acceptance_criteria>
  <done>Auth endpoint throttling and production cookie settings have focused automated regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~Auth"` passes.
- [ ] `dotnet test LineCom.sln --no-restore --filter "FullyQualifiedName~Auth"` passes or any restore/network blocker is recorded in the summary.
- [ ] Manual code inspection confirms no Phase 1 rate limiting was applied outside login/register.
</verification>

<success_criteria>
- SEC-01 is covered by tested 429 behavior on login/register.
- SEC-02 is covered by production cookie option verification.
- D-01: Login/register are the only Phase 1 throttled endpoints.
- D-02: Rate limiting key uses IP + endpoint.
- D-03: Rate limiting starts at 5 attempts / 1 minute.
- D-04: Rejection response is 429 with code auth.rate_limited.
- D-05: Me/logout/request submit are not included in Phase 1 throttling.
- No database schema or Local FileStorage behavior changes are introduced by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-safety-baseline/01-01-SUMMARY.md`.
</output>
