---
phase: 04-public-seo-geo-reliability
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/front/src/app/sitemap.ts
  - apps/front/src/app/sitemap.test.ts
  - vault/Человекочитаемое/SEO GEO Public Catalog.md
autonomous: false
requirements:
  - SEO-02
  - SEO-03
user_setup: []
must_haves:
  truths:
    - "D-01: Keep Phase 4 on a single public /sitemap.xml; do not implement segmented sitemap files."
    - "D-02: Sitemap product enumeration must be explicitly bounded by maximum product API pages and maximum product URLs."
    - "D-03: When the catalog exceeds the Phase 4 release limit, sitemap generation returns a valid truncated sitemap instead of failing or enumerating without bound."
    - "D-04: Truncation behavior and release limit must be documented; segmented sitemaps via generateSitemaps are deferred."
    - "D-05: Sitemap limit constants should live near app/sitemap.ts and be covered by route-level tests; do not introduce env/config limits."
    - "D-12: robots.ts continues pointing to a single absolute /sitemap.xml because Phase 4 uses a bounded single sitemap."
    - "Do not add SEO landing pages or segmented sitemap routes in this plan."
  artifacts:
    - path: apps/front/src/app/sitemap.ts
      provides: "Bounded single sitemap generation"
      contains: "SITEMAP_MAX_PRODUCT_PAGES|SITEMAP_MAX_PRODUCT_URLS"
    - path: apps/front/src/app/sitemap.test.ts
      provides: "Route-level sitemap limit and fallback tests"
      contains: "max|truncat|page"
    - path: vault/Человекочитаемое/SEO GEO Public Catalog.md
      provides: "Source-of-truth documentation for Phase 4 bounded sitemap release limit"
      contains: "release limit|generateSitemaps"
  key_links:
    - from: apps/front/src/app/sitemap.ts
      to: apps/front/src/lib/api/catalog.ts
      via: "getProducts page/pageSize calls"
      pattern: "getProducts"
    - from: apps/front/src/app/sitemap.ts
      to: apps/front/src/lib/seo/sitemap.ts
      via: "bounded product list passed to buildPublicSitemapEntries"
      pattern: "buildPublicSitemapEntries"
---

<objective>
Make public sitemap generation scalable enough for release growth by keeping a single `/sitemap.xml` but bounding product enumeration and documenting the release limit.

User story: As a release operator, I want sitemap generation to stay predictable as the catalog grows so crawler requests do not trigger unbounded public API enumeration.

Purpose: Cover `SEO-02` and sitemap parts of `SEO-03` using the bounded single-sitemap strategy selected in Phase 4 context.
Output: Bounded product loading in `app/sitemap.ts`, route-level tests for truncation/page limits, and source-of-truth documentation for the release limit and future segmented path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-public-seo-geo-reliability/04-CONTEXT.md
@.planning/phases/04-public-seo-geo-reliability/04-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@vault/Человекочитаемое/SEO GEO Public Catalog.md
@apps/front/src/app/sitemap.ts
@apps/front/src/app/sitemap.test.ts
@apps/front/src/lib/seo/sitemap.ts
@apps/front/src/lib/seo/sitemap.test.ts
@apps/front/src/lib/api/catalog.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-04-05: sitemap request enumerates every product page as catalog grows | high | Add max API-page and max product-URL constants and tests proving calls stop at the limit. |
| T-04-06: sitemap fails entirely when product API branch is too large or unavailable | medium | Preserve partial fallback behavior and return valid static/category entries when product loading fails. |
| T-04-07: truncation silently becomes permanent architecture | medium | Document the Phase 4 release limit and future `generateSitemaps` segmented path in source-of-truth docs. |
| T-04-08: limits become hidden runtime config with untested production variance | low | Use named constants near `app/sitemap.ts`; do not introduce env/config knobs in this phase. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add visible release limit constants to sitemap route</name>
  <files>apps/front/src/app/sitemap.ts</files>
  <read_first>apps/front/src/app/sitemap.ts, apps/front/src/lib/api/catalog.ts, .planning/phases/04-public-seo-geo-reliability/04-CONTEXT.md, .planning/phases/04-public-seo-geo-reliability/04-RESEARCH.md</read_first>
  <action>Add named constants near the existing `SITEMAP_PRODUCT_PAGE_SIZE`, such as `SITEMAP_MAX_PRODUCT_PAGES` and `SITEMAP_MAX_PRODUCT_URLS`. Keep `SITEMAP_PRODUCT_PAGE_SIZE = 60` because the public API supports page sizes up to 60. Do not move these limits into env/config. Choose conservative values that make Phase 4 bounded and easy to test.</action>
  <verify>npm.cmd --prefix apps/front test -- src/app/sitemap.test.ts</verify>
  <acceptance_criteria>
    - Product sitemap limits are visible in `apps/front/src/app/sitemap.ts`.
    - Existing product page size remains backend-supported at 60.
    - No env/config surface is added for sitemap limits.
  </acceptance_criteria>
  <done>Sitemap route exposes deterministic release limit constants.</done>
</task>

<task type="auto">
  <name>Task 2: Bound product page loading and URL collection</name>
  <files>apps/front/src/app/sitemap.ts</files>
  <read_first>apps/front/src/app/sitemap.ts, apps/front/src/lib/seo/sitemap.ts</read_first>
  <action>Update `loadSitemapProducts` so it loads at most the configured product page limit and collects at most the configured product URL limit. Stop loading additional pages when either limit is reached. Keep category loading independent through `Promise.allSettled`, and preserve current fallback behavior where a failed product branch still allows static/category sitemap entries.</action>
  <verify>npm.cmd --prefix apps/front test -- src/app/sitemap.test.ts</verify>
  <acceptance_criteria>
    - `getProducts` is never called beyond `SITEMAP_MAX_PRODUCT_PAGES`.
    - Returned product list never exceeds `SITEMAP_MAX_PRODUCT_URLS`.
    - Static and category entries continue to be generated when product loading fails.
    - Sitemap remains a valid single `MetadataRoute.Sitemap` array.
  </acceptance_criteria>
  <done>Sitemap product enumeration is explicitly bounded.</done>
</task>

<task type="auto">
  <name>Task 3: Add sitemap truncation and fallback regression tests</name>
  <files>apps/front/src/app/sitemap.test.ts</files>
  <read_first>apps/front/src/app/sitemap.test.ts, apps/front/src/app/sitemap.ts, apps/front/src/lib/seo/sitemap.test.ts</read_first>
  <action>Extend route-level sitemap tests to cover max page count, max product URL count, and partial fallback behavior. Use mocked `getProducts` responses with inflated `totalPages` and more product items than the release URL limit. Assert the test fails if implementation keeps walking all `totalPages` or includes product URLs beyond the release limit.</action>
  <verify>npm.cmd --prefix apps/front test -- src/app/sitemap.test.ts src/lib/seo/sitemap.test.ts</verify>
  <acceptance_criteria>
    - Tests prove API calls stop at the max page limit.
    - Tests prove output product URLs stop at the max product URL limit.
    - Existing static/category/product URL generation and deduplication tests still pass.
  </acceptance_criteria>
  <done>Bounded sitemap behavior has route-level regression coverage.</done>
</task>

<task type="auto">
  <name>Task 4: Document Phase 4 bounded sitemap release limit</name>
  <files>vault/Человекочитаемое/SEO GEO Public Catalog.md</files>
  <read_first>vault/Человекочитаемое/SEO GEO Public Catalog.md, .planning/phases/04-public-seo-geo-reliability/04-CONTEXT.md, .planning/phases/04-public-seo-geo-reliability/04-RESEARCH.md</read_first>
  <action>Update the sitemap section to document that Phase 4 keeps a single `/sitemap.xml` with explicit product API page and product URL release limits. State that exceeding the limit produces a valid truncated sitemap and that segmented sitemap files via Next.js `generateSitemaps` are deferred as the future growth path. Do not add landing page behavior or segmented sitemap URLs to the current contract.</action>
  <verify>rg "generateSitemaps|release limit|sitemap" "vault/Человекочитаемое/SEO GEO Public Catalog.md"</verify>
  <acceptance_criteria>
    - Source-of-truth doc explains bounded single sitemap behavior.
    - Source-of-truth doc explicitly defers segmented sitemap implementation.
    - Documentation does not introduce SEO landing pages into Phase 4 scope.
  </acceptance_criteria>
  <done>Bounded sitemap behavior is documented for downstream verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm.cmd --prefix apps/front test -- src/app/sitemap.test.ts src/lib/seo/sitemap.test.ts` passes.
- [ ] Source inspection confirms `loadSitemapProducts` has both page and URL bounds.
- [ ] Source inspection confirms `robots.ts` still references only `/sitemap.xml`.
- [ ] `vault/Человекочитаемое/SEO GEO Public Catalog.md` documents the bounded release limit and future segmented path.
</verification>

<success_criteria>
- SEO-02 is covered by bounded sitemap generation.
- SEO-03 is covered by sitemap route/helper regression tests.
- D-01 through D-05 are implemented and tested.
- D-12 remains true: robots points to a single sitemap because Phase 4 remains single-sitemap.
- Sitemap growth beyond the release limit is documented as valid truncation plus deferred segmentation, not accidental unbounded enumeration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-seo-geo-reliability/04-02-SUMMARY.md`.
</output>
