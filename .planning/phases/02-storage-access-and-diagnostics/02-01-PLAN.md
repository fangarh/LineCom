---
phase: 02-storage-access-and-diagnostics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs
  - apps/api/Infrastructure/Hosting/LocalStoragePathPolicy.cs
autonomous: true
requirements:
  - STOR-01
  - STOR-02
user_setup: []
must_haves:
  truths:
    - "D-01: Public catalog images remain available through a restricted static-file path, while private-purpose files require a separate authorized path/controller if download support is ever needed."
    - "D-02: Anonymous static serving is limited to current public image directories: storage/products/... and storage/brands/...."
    - "D-03: Existing public image URLs must be preserved; do not mass-change stored_files.storage_key, frontend URLs, or public catalog DTOs."
    - "D-04: The boundary must be proven with test-host/static-file integration tests for allowed public image directories and private-like import/export/temp directories."
    - "Local FileStorage remains the target storage approach."
    - "Do not implement Phase 3 import staging, promotion, cleanup, reset, or destructive storage maintenance."
  artifacts:
    - path: apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs
      provides: "Restricted public static-file mapping for Local FileStorage"
      contains: "/storage/products|/storage/brands"
    - path: apps/api/Infrastructure/Hosting/LocalStoragePathPolicy.cs
      provides: "Shared storage root and public-prefix policy, if extracted during execution"
      contains: "products|brands"
  key_links:
    - from: apps/api/Program.cs
      to: apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs
      via: "Program keeps calling UseLocalStorageStaticFiles"
      pattern: "UseLocalStorageStaticFiles"
    - from: apps/api/Modules/Catalog/Repositories/AdminCatalogImageSql.cs
      to: "/storage/products/..."
      via: "product image URLs are emitted as '/' || stored_file.storage_key"
      pattern: "stored_file.storage_key"
    - from: apps/api/Modules/Catalog/Repositories/AdminCatalogBrandSql.cs
      to: "/storage/brands/..."
      via: "brand logo URLs are emitted as '/' || stored_file.storage_key"
      pattern: "stored_file.storage_key"
---

<objective>
Restrict anonymous Local FileStorage static serving to the current public catalog image directories while preserving existing public image URLs.

User story: As a release operator, I want `/storage` to serve only intended public catalog images so that future import/export/temp files under Local FileStorage cannot be fetched anonymously.

Purpose: Cover `STOR-01`, `STOR-02`, and Phase 2 decisions D-01 through D-04 without changing storage keys, catalog DTOs, or the target Local FileStorage approach.
Output: A restricted static-file serving policy that keeps public image URL compatibility. Focused boundary tests are owned by dependent Plan 02-03.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-storage-access-and-diagnostics/02-CONTEXT.md
@.planning/phases/02-storage-access-and-diagnostics/02-RESEARCH.md
@.planning/phases/01-release-safety-baseline/01-VERIFICATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@apps/api/Program.cs
@apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs
@apps/api/Infrastructure/Storage/LocalStoredFileWriter.cs
@apps/api/Infrastructure/Database/DatabaseServiceCollectionExtensions.cs
@apps/api/Modules/Catalog/Repositories/AdminCatalogImageSql.cs
@apps/api/Modules/Catalog/Repositories/AdminCatalogBrandSql.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace broad storage-root static serving with public-prefix serving</name>
  <files>apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs, apps/api/Infrastructure/Hosting/LocalStoragePathPolicy.cs</files>
  <read_first>apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs, apps/api/Program.cs, apps/api/Infrastructure/Storage/LocalStoredFileWriter.cs, .planning/phases/02-storage-access-and-diagnostics/02-CONTEXT.md</read_first>
  <action>Change `UseLocalStorageStaticFiles` so it no longer maps the entire resolved storage root to `/storage`. Keep the existing root resolution behavior and directory creation behavior, but register public static files only for the current public image prefixes. Requests to `/storage/products/...` must resolve to the physical `<root>/products/...` subtree and requests to `/storage/brands/...` must resolve to `<root>/brands/...`. Do not introduce `/storage/public`, do not rewrite storage keys, and do not change the `Program.cs` call site unless needed for a narrow helper signature. If a helper is useful, create a small `LocalStoragePathPolicy` under `Infrastructure/Hosting` for root resolution and public-prefix constants.</action>
  <verify>dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~LocalStoredFileWriter"</verify>
  <acceptance_criteria>
    - `/storage/products/...` and `/storage/brands/...` remain valid external URL prefixes.
    - No static-file registration serves the whole storage root at `/storage`.
    - Root path resolution still supports missing `Storage:RootPath` by falling back to `apps/api/storage` outside production.
    - Directory traversal stays delegated to `PhysicalFileProvider` subroots and cannot escape the selected public subdirectories.
  </acceptance_criteria>
  <done>Anonymous static serving is scoped to the two public image subtrees while current URL shape is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Keep public catalog URL compatibility explicit</name>
  <files>apps/api/Infrastructure/Hosting/LocalStorageStaticFilesExtensions.cs, apps/api/Modules/Catalog/Repositories/AdminCatalogImageSql.cs, apps/api/Modules/Catalog/Repositories/AdminCatalogBrandSql.cs</files>
  <read_first>apps/api/Modules/Catalog/Repositories/AdminCatalogImageSql.cs, apps/api/Modules/Catalog/Repositories/AdminCatalogBrandSql.cs, apps/api/Modules/Catalog/Queries/PublicProductSql.cs, .planning/phases/02-storage-access-and-diagnostics/02-CONTEXT.md</read_first>
  <action>Inspect catalog SQL/DTO paths that build public product image and brand logo URLs. Confirm they still emit `"/" || stored_file.storage_key` and that stored keys beginning with `storage/products/` or `storage/brands/` continue to match the static-file registrations from Task 1. Do not change public catalog DTOs, frontend URL construction, or stored file key shape. If no code change is needed in SQL files, leave them untouched and document the compatibility check in the plan summary.</action>
  <verify>rg \"'/' \\|\\| stored_file.storage_key|storage_key\" apps/api/Modules/Catalog</verify>
  <acceptance_criteria>
    - Public product image URLs continue to be `/storage/products/...`.
    - Public brand logo URLs continue to be `/storage/brands/...`.
    - No migration or mass update to `stored_files.storage_key` is introduced.
  </acceptance_criteria>
  <done>Catalog image URL compatibility has been inspected and preserved.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~LocalStoredFileWriter"` passes.
- [ ] After dependent Plan 02-03, `dotnet test tests/LineCom.Api.Tests/LineCom.Api.Tests.csproj --filter "FullyQualifiedName~LocalStorageStaticFiles"` passes.
- [ ] Manual inspection confirms no public catalog DTO, frontend image URL, or `stored_files.storage_key` migration was introduced.
- [ ] Manual inspection confirms import/export/temp static paths are not served anonymously.
</verification>

<success_criteria>
- STOR-01 is covered by static serving limited to intended public catalog image paths.
- STOR-02 is covered by anonymous 404 behavior for private-like storage paths.
- D-01: Public images remain static; private-purpose downloads are deferred to a future authorized path/controller.
- D-02: Only current `products` and `brands` public image directories are anonymously served.
- D-03: Existing public image URLs remain stable.
- D-04: Test-host integration tests are explicitly delegated to dependent Plan 02-03.
- No Phase 3 import staging/cleanup or destructive storage maintenance is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-storage-access-and-diagnostics/02-01-SUMMARY.md`.
</output>
