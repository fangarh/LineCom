---
phase: 01-release-safety-baseline
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/front/src/lib/api/http.ts
  - apps/front/src/lib/api/errors.ts
  - apps/front/src/lib/api/errors.test.ts
  - apps/front/src/lib/api/http.test.ts
autonomous: true
requirements:
  - API-01
  - API-02
user_setup: []
must_haves:
  truths:
    - "D-11: apiJson and apiForm throw ApiClientError with code transport.invalid_response for non-JSON, empty error body, and malformed JSON responses."
    - "D-12: The user-facing invalid response message is `Не удалось обработать ответ сервера. Попробуйте позже.`"
    - "D-13: Technical invalid-response details are available only for diagnostics and are not exposed through normalizeApiError output."
    - "D-14: apiJson and apiForm use the same parsing helper for invalid-response behavior."
  artifacts:
    - path: apps/front/src/lib/api/http.ts
      provides: "Shared API response parsing helper used by apiJson and apiForm"
      contains: "transport.invalid_response"
    - path: apps/front/src/lib/api/errors.ts
      provides: "ApiClientError diagnostics support without exposing raw details to users"
      contains: "ApiClientError"
    - path: apps/front/src/lib/api/http.test.ts
      provides: "JSON and multipart invalid response regression tests"
      contains: "transport.invalid_response"
  key_links:
    - from: apps/front/src/lib/api/http.ts
      to: apps/front/src/lib/api/errors.ts
      via: "throws ApiClientError from shared parsing helper"
      pattern: "new ApiClientError"
    - from: apps/front/src/lib/api/http.ts
      to: "apiJson and apiForm"
      via: "both helpers call the same parse response function"
      pattern: "apiJson|apiForm"
---

<objective>
Normalize frontend API transport failures so proxy/upstream invalid responses become controlled client errors.

User story: As a user submitting catalog/admin/account actions, I want infrastructure response failures to show a stable retry message instead of raw parser or proxy details.

Purpose: Cover decisions D-11 through D-14 and requirements API-01/API-02 for both JSON and multipart API helpers.
Output: Shared response parsing helper, `ApiClientError` invalid-response diagnostics support, and Vitest coverage for JSON/form transport failures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-release-safety-baseline/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@apps/front/src/lib/api/http.ts
@apps/front/src/lib/api/errors.ts
@apps/front/src/lib/api/errors.test.ts
@apps/front/src/lib/api/admin-catalog.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared response parsing helper for JSON and form requests</name>
  <files>apps/front/src/lib/api/http.ts</files>
  <read_first>apps/front/src/lib/api/http.ts, apps/front/src/lib/api/errors.ts, .planning/phases/01-release-safety-baseline/01-CONTEXT.md</read_first>
  <action>Refactor the duplicated `response.text()` + `JSON.parse` logic in `apiJson` and `apiForm` into one shared helper inside `http.ts`, for example `parseApiResponse<T>(response: Response): Promise<T>`. Keep 204 returning `undefined as T`. For any non-204 response where the body is empty, non-JSON, or malformed JSON, throw `ApiClientError` with status `response.status`, code `transport.invalid_response`, and message `Не удалось обработать ответ сервера. Попробуйте позже.`. For non-ok responses with a valid `{ code, message }` API payload, preserve the existing backend error code/message behavior. For ok responses with malformed or empty non-204 bodies, also throw `transport.invalid_response` because the client cannot safely consume the response. Ensure both `apiJson` and `apiForm` call this same helper.</action>
  <verify>npm --prefix apps/front test -- src/lib/api/http.test.ts src/lib/api/errors.test.ts</verify>
  <acceptance_criteria>
    - `apiJson` no longer has its own separate `JSON.parse` branch duplicated from `apiForm`.
    - `apiForm` no longer has its own separate `JSON.parse` branch duplicated from `apiJson`.
    - The shared helper throws `transport.invalid_response` for invalid response parsing cases.
    - Valid backend API error payloads still produce their original backend error codes.
  </acceptance_criteria>
  <done>Both frontend API request helpers use one invalid-response parsing path.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve diagnostics without exposing raw details to users</name>
  <files>apps/front/src/lib/api/errors.ts, apps/front/src/lib/api/errors.test.ts</files>
  <read_first>apps/front/src/lib/api/errors.ts, apps/front/src/lib/api/errors.test.ts, apps/front/src/lib/api/http.ts</read_first>
  <action>Extend `ApiClientError` only as needed to preserve diagnostic details for invalid responses, such as a `cause` or readonly diagnostic metadata field containing parse failure/body/status information. Do not include raw HTML, plaintext proxy responses, or JSON parser messages in `error.message`, `normalizeApiError(error).message`, or any user-facing normalized response. Add tests proving `normalizeApiError(new ApiClientError(...))` returns only the stable code/message and not the technical diagnostic details.</action>
  <verify>npm --prefix apps/front test -- src/lib/api/errors.test.ts</verify>
  <acceptance_criteria>
    - `ApiClientError` can carry invalid-response diagnostics for tests/logging.
    - `normalizeApiError` returns `{ code: "transport.invalid_response", message: "Не удалось обработать ответ сервера. Попробуйте позже." }` for invalid response errors.
    - Tests prove raw invalid response details are not present in the normalized message.
  </acceptance_criteria>
  <done>Diagnostic details are retained without being shown to users.</done>
</task>

<task type="auto">
  <name>Task 3: Add JSON and multipart invalid-response tests</name>
  <files>apps/front/src/lib/api/http.test.ts, apps/front/src/lib/api/admin-catalog.test.ts</files>
  <read_first>apps/front/src/lib/api/admin-catalog.test.ts, apps/front/src/lib/api/http.ts, apps/front/src/lib/api/errors.ts</read_first>
  <action>Create `apps/front/src/lib/api/http.test.ts` if it does not exist. Stub `fetch` with Vitest and cover `apiJson` and `apiForm` for: non-JSON HTML/plaintext error response, empty 500 response, malformed JSON response, valid backend API error response, and 204 no-content response. At least one multipart-facing test should use `FormData` through `apiForm` directly or an existing multipart wrapper such as `uploadAdminBrandLogo`, and must assert `transport.invalid_response` plus the stable retry message. Preserve existing tests that assert fallback `internal_error` for syntactically valid JSON bodies that are not API error shapes only if the new Phase 1 decision does not supersede that exact case; otherwise update them to the new `transport.invalid_response` contract.</action>
  <verify>npm --prefix apps/front test -- src/lib/api/http.test.ts src/lib/api/errors.test.ts src/lib/api/admin-catalog.test.ts</verify>
  <acceptance_criteria>
    - Tests fail on the pre-change raw `SyntaxError` behavior.
    - Tests cover both `apiJson` and `apiForm`.
    - Tests assert `transport.invalid_response` and `Не удалось обработать ответ сервера. Попробуйте позже.`.
    - Existing admin API tests still prove JSON/form headers and CSRF behavior are unchanged.
  </acceptance_criteria>
  <done>Frontend API transport invalid-response behavior is covered for JSON and multipart request paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix apps/front test -- src/lib/api/http.test.ts src/lib/api/errors.test.ts src/lib/api/admin-catalog.test.ts` passes.
- [ ] `npm --prefix apps/front test -- src/lib/api` passes if Vitest accepts the directory target; otherwise record the exact command used.
- [ ] `npm --prefix apps/front run lint` passes or unrelated pre-existing lint failures are documented in the summary.
</verification>

<success_criteria>
- API-01 is covered by controlled `transport.invalid_response` behavior for non-JSON, empty, and malformed responses.
- API-02 is covered by tests for both JSON and multipart request helpers.
- D-11: Invalid backend/proxy responses throw ApiClientError code transport.invalid_response.
- D-12: Invalid responses use the stable retry message `Не удалось обработать ответ сервера. Попробуйте позже.`
- D-13: Technical details are preserved only for diagnostics and not shown to users.
- D-14: apiJson and apiForm share the same parsing helper.
- No public UI surface exposes raw proxy HTML, plaintext upstream body, or JSON parser details.
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-safety-baseline/01-03-SUMMARY.md`.
</output>
